{
  "name": "nodewebapp",
  "private": true,
  "version": "1.0.0",
  "description": "Node webapp boilerplate",
  "main": "app.js",
  "scripts": {
	"clean": "rm -rf build && mkdir build",
	"compile": "babel -d build src -s",
	"build": "npm run clean && npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.13.3",
    "express": "^4.13.3",
    "pg": "^4.4.1",
    "pg-hstore": "^2.3.2",
    "sequelize": "^3.6.0"
  }
}
